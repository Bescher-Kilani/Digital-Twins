logging.level.root=INFO

server.port=${SERVER_PORT:9090}

spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=${MONGODB_DATABASE:DigiTwinStudio}

spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_URL:http://localhost:8080}/realms/DigiTwinStudio
logging.level.org.springframework.security=DEBUG

logging.level.org.eclipse.digitaltwin.aas4j.v3.dataformat.aasx.AASXSerializer=error

# Scheduling Configuration
# Controls whether scheduled tasks (GuestCleanupService, TemplateService) are enabled
# Default: true (enabled for local development)
# Set SCHEDULING_ENABLED=false in Railway to allow service to sleep
scheduling.enabled=${SCHEDULING_ENABLED:true}


startup.initialization.enabled=${STARTUP_INIT_ENABLED:true}


management.endpoints.enabled-by-default=false
management.endpoint.health.enabled=false
MONGODB_URI="mongodb+srv://bescherkilani:galaxy69@digitwin-cluster.s86xfiw.mongodb.net/?appName=digitwin-cluster"